# Optional path override for local source sync.
GEORGEHOOKS_PATH=../georgehooks

# Optional deterministic feed generation.
HOOKS_RADAR_FEED_DATE=2026-06-27
HOOKS_RADAR_FEED_RESET=0

# Server-side lead capture for the install unlock form.
# Create public.radar_leads with docs/radar-leads-supabase.sql.
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<server-side-service-role-key>

# Server-side GitHub request intake. Do not prefix these with NEXT_PUBLIC_.
GITHUB_TOKEN=<server-side-github-token>
GITHUB_OWNER=georgewangyu
GITHUB_REPO=audience-request-form
GITHUB_PRIVATE_REPO=audience-private-intake
GITHUB_API_VERSION=2022-11-28
HOOKS_RADAR_REQUEST_ALLOWED_ORIGIN=
