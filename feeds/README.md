# Hooks Radar Feeds

Weekly feed files are generated by:

```sh
npm run feed:weekly
```

The latest feed can be printed with:

```sh
npm run feed:latest
```

Feeds are source-backed summaries of the public-ready catalog. The source of
truth remains `../georgehooks/hooks/*.md`.

